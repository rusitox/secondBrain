---
allowed-tools: Read, Write, Glob
description: Create or update project rules in .claude/rules/
---

1. Ask what convention to enforce and which files it applies to
2. Create `.claude/rules/<rule-name>.md` with globs frontmatter
3. Write clear, specific, actionable rules with DO/DON'T examples
4. Examples: error-handling, component-style, api-patterns, testing, imports
